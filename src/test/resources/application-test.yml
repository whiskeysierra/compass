spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:11://localhost/postgres?currentSchema=compass

riptide.clients:
  local:
    base-url: http://localhost:8080
    socket-timeout: 10 minutes
